# deps
node_modules/
.pnpm-store/

# build output
dist/
build/

# env
.env
.env.local
*.env

# data (sqlite + baileys auth state)
backend/data/

# logs
*.log
logs/
deploy/logs/

# editors / OS
.DS_Store
.vscode/
.idea/
Thumbs.db

# misc
*.tsbuildinfo
coverage/

# LLM-generated planning artifacts. CLAUDE.md and README are the only docs
# kept in-tree: they are context the next reader needs, not a record of how a
# change was produced. Plans and specs go stale the moment the code moves and
# nothing reconciles them.
docs/superpowers/
.superpowers/
